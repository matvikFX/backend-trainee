package server

import "github.com/labstack/echo/v4"

func (s *Server) MapHandlers(e *echo.Echo) error {
	// Init repositories
	// Написать репоситорий для баннеров, потому что других нет

	// Init useCases
	// Если такие есть, надо написать все здесь, потому что других нет

	// Init handlers
	// Написать handler'ы, потому что больше у меня быть не может

	return nil
}
